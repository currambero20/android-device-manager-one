{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "cleanUrls": true,
  "installCommand": "npm install",
  "build": {
    "env": {
      "NODE_OPTIONS": "--require tsx"
    },
    "includeFiles": [
      "server/**/*",
      "shared/**/*"
    ]
  },
  "framework": null,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/vercel.ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}